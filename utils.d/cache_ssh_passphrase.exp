#!/usr/bin/expect -f

set sshkey_path [lindex $argv 0]
set passphrase [lindex $argv 1]

set timeout 10

spawn ssh-add $sshkey_path
expect "Enter passphrase for $sshkey_path"
send "$passphrase\r"
expect eof
