### <ctrl-y> copy to clipboard + nord theme



$env:FZF_DEFAULT_OPTS = @"
--color=fg:#e5e9f0,bg:#3b4252,hl:#81a1c1
--color=fg+:#e5e9f0,bg+:#3b4252,hl+:#81a1c1
--color=info:#eacb8a,prompt:#bf6069,pointer:#b48dac
--color=marker:#a3be8b,spinner:#b48dac,header:#a3be8b
--bind=ctrl-y:execute-silent(echo {} | clip)
--header=<Ctrl-Y> to copy to clipboard
"@

## NOTE/EDGECASE

# only copy current line when --multi flag passed
--bind=ctrl-y:execute-silent(echo {} | clip)

# copy all multi-selected items
--bind=ctrl-y:execute-silent(echo {+} | clip)


## other flags to add

--highlight-line 
--scroll-off=2



--ghost="N/A N/A"


--filepath-word   ## curious


-1

-0


##  alt-c preview dir
$env:FZF_ALT_C_OPTS = '--preview=powershell -NoProfile -Command "Get-ChildItem -Force \"{}\" | Format-Table Mode, LastWriteTime, Length, Name -AutoSize | Out-String"'
